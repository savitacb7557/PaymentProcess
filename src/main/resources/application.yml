spring:
  application:
    name: PAYMENTPROCESS
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false
    mapper:
      # Accept "upi", "UPI", "Upi", etc. for enums
      accept-case-insensitive-enums: true

server:
  # Port 8763 is standard for Microservices; choose one and delete the other
  port: 8763

management:
  endpoints:
    web:
      exposure:
        include: health,info,mappings
  endpoint:
    health:
      show-details: always

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    instance-id: ${spring.application.name}:${random.value}
    prefer-ip-address: true

logging:
  level:
    org.springframework.web: DEBUG
    org.springframework.http.converter.json: DEBUG
    org.springframework.validation: DEBUG
    com.tcs.payments: DEBUG

# --- Payments Configuration ---
payments:
  # Define regions only once
  supported-regions:
    - code: "US"
      currency: "USD"
      provider: "stripe"
    - code: "IN"
      currency: "INR"
      provider: "razorpay"

  stripe:
    secretKey: "sk_test_51ShunDFXsdj6jjALGm7OSZqneZ1ZG5CXhOfQ59bvwgrO1BJNdpSkrH9djUzy9cFpz5ovYaWSBdOyX87JupLXRdt7006SCBYgLH"
    webhookSecret: ""

  razorpay:
    keyId: "rzp_test_Rvk6pvAcQslNoh"
    keySecret: "tMGbcjh0LbWv0On4u70Wbd5k"

  security:
    # Your 32-byte AES key (Base64) used for PCI metadata (AES-256-GCM)
    pciMetaKeyBase64: "mB0Ltl3zlH8BFn6Xp0biUHk4owazHPF4In8GKa8v6S0="